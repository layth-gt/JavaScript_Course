var ordinals = ["zeroth", "first", "second", "third"];

console.log (ordinals[0]);

//Count from 0 to 3 with a loop

for (var count = 0; count < 3; count +=1) {
    var currentOrdinal = ordinals[count];
    console.log (currentOrdinal);
}